#include "main.h"
#include <string.h>
/**
 * _strcmp - cocnate two strings
 * @s1: char type
 * @s2: char
 * Return: value
 */
int _strcmp(char *s1, char *s2)
{
	int a;

	a = strcmp(s1, s2);
	return (a);
}
