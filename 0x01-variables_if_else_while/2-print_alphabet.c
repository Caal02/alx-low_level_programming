#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints lower alphabets
 * Return: value of 0
 */
int main(void)
{
	int i;

	for (i = 97; i <= 122; i++)
	{
	putchar(i);
	}
	putchar(10);
	return (0);
}
