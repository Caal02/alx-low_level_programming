#include <stdio.h>
/**
 * main - prints number in base 10
 * Return: value of 0
*/
int main(void)
{
	int a;

	for (a = 48; a <= 56; a++)
	{
	putchar(a);
	putchar(44);
	putchar(32);
	}
	putchar(57);
	return (0);
}
