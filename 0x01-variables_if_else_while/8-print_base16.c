#include <stdio.h>
/**
 * main - prints number in base 10
 * Return: value of 0
*/
int main(void)
{
	int a;
	int b;

	for (a = 48; a <= 57; a++)
	{
	putchar(a);
	}
	for (b = 97; b <= 102; b++)
	{
	putchar(b);
	}
	putchar(10);
	return (0);
}
