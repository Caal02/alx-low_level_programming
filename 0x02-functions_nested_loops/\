#include <stdio.h>
/**
  *main - calulat multiple and sum of 3&5
  *return: no value
 */

int main(void);
{
	int i,a,b,c;

	for(i = 0; i <= 1024; i++)
	{
		if (i / 3 == 0)
			a = i;
		if (i / 5 == 0)
			b = i;
		c = a + b;
	}
		printf ("%d", c)
	return (0);
}
