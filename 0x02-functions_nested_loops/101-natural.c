#include <stdio.h>
/**
  *main - calulat multiple and sum of 3&5
  *Return: no value
 */

int main(void);
{
	int i, a, b, c;

	for (i = 1; i < = 1024; i++)
	{
		if (i / 3 == 0)
			a = i;
		if (i / 5 == 0)
			b = i;
		c = a + b;
	}
		printf("%d\n", c);
	return (0);
}
